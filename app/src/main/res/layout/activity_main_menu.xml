<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7EBEC"
    android:orientation="vertical"
    tools:context=".MainMenu">

    <TextView
        android:id="@+id/mainTitleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="50dp"
        android:fontFamily="@font/milkhoney"
        android:textAlignment="center"
        android:textColor="#1D1E2C"
        android:textSize="40sp"
        android:text="Disease in the Inland Empire"
       />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager2"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginBottom="50dp"
        android:padding="20dp"
        android:paddingStart="00dp" />

    <Button
        android:id="@+id/mainStartBtn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="40dp"
        android:background="@drawable/button"
        android:backgroundTint="#AC9FBB"
        android:fontFamily="@font/lazydog"
        android:gravity="center"
        android:text="Start"
        android:textColor="#1D1E2C"
        android:textSize="20sp"
        android:typeface="monospace" />

    <ImageView
        android:id="@+id/logoImg"
        android:layout_width="123dp"
        android:layout_height="106dp"
        android:layout_gravity="center"
        app:srcCompat="@drawable/caclogo" />

</LinearLayout>